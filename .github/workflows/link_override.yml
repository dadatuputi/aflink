name: Link Override from Issue

on:
  repository_dispatch:
    types: [link_override]
  
jobs:
  override_link:
    runs-on: ubuntu-latest
    steps:
      
    - name: parse_fields
      env:
        BODY: ${{ github.event.client_payload.issue_body }}
      run: |

        echo "Original issue: ${{ github.event.client_payload.original_issue }}"
        echo "Triggered by: ${{ github.event.client_payload.triggered_by }}"
        echo "Issue body: ${{ github.event.client_payload.issue_body }}"
         
        # Parse the override issue body format (Issue template now supplies Link ID).
        # Anchor on the "### <header>" line and take the first non-blank line after it,
        # trimmed in-process. This avoids xargs (which crashes on apostrophes/quotes,
        # e.g. "Bob's Portal") and tolerates blank-line shifts and CRLF payloads.
        field() {
          printf '%s' "$BODY" | awk -v H="### $1" '
            index($0, H) == 1 { found = 1; next }
            found && $0 ~ /[^[:space:]]/ {
              sub(/^[[:space:]]+/, ""); sub(/[[:space:]]+$/, ""); print; exit
            }'
        }

        LINK_ID=$(field "Link ID")
        NEW_TITLE=$(field "New Link Title")
        NEW_LINK=$(field "New Link URL")
        CHANGE_TYPE=$(field "What are you updating?")

        # Blank optional form fields arrive as the literal "_No response_"
        [[ "$NEW_TITLE" == "_No response_" ]] && NEW_TITLE=""
        [[ "$NEW_LINK" == "_No response_" ]] && NEW_LINK=""

        # Validate required fields
        if [[ -z "$LINK_ID" ]]; then
          echo "VALIDATION_ERROR=Missing link id" >> $GITHUB_ENV
          exit 1
        fi

        # Export to environment
        echo "LINK_ID=$LINK_ID" >> $GITHUB_ENV
        echo "NEW_TITLE=$NEW_TITLE" >> $GITHUB_ENV
        echo "NEW_LINK=$NEW_LINK" >> $GITHUB_ENV

        # The selected change type dictates which new-value fields must be present;
        # a later step comments the error back on the issue and fails the job.
        if [[ "$CHANGE_TYPE" == *"Title"* && -z "$NEW_TITLE" ]]; then
          echo "VALIDATION_ERROR=the change type includes the title, but no New Link Title was provided" >> $GITHUB_ENV
        elif [[ "$CHANGE_TYPE" == *"URL"* && -z "$NEW_LINK" ]]; then
          echo "VALIDATION_ERROR=the change type includes the URL, but no New Link URL was provided" >> $GITHUB_ENV
        elif [[ -z "$NEW_TITLE" && -z "$NEW_LINK" ]]; then
          echo "VALIDATION_ERROR=no New Link Title or New Link URL was provided" >> $GITHUB_ENV
        fi

        echo "✅ Parsed values:"
        echo "  Link ID: $LINK_ID"
        echo "  New Title: $NEW_TITLE"
        echo "  New URL: $NEW_LINK"
          
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install jq & sponge
      run: |
        # Skip installing pacakge docs {makes the man-db trigger much faster) 
        # disabled `/doc` and `/info` as well to speed up apt operations further
        sudo tee /etc/dpkg/dpkg.cfg.d/01_nodoc > /dev/null << 'EOF'
        path-exclude /usr/share/doc/*
        path-exclude /usr/share/man/*
        path-exclude /usr/share/info/*
        EOF

        sudo apt-get update
        sudo apt-get install --yes jq moreutils uuid-runtime

    - name: Fail on validation error
      if: env.VALIDATION_ERROR != ''
      uses: actions/github-script@v6
      with:
        script: |
          await github.rest.issues.createComment({
            issue_number: context.payload.client_payload.original_issue,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `❌ Failed to add override: ${{ env.VALIDATION_ERROR }}. Please edit the issue so the override can be created.`
          });
          throw new Error('Validation error: ${{ env.VALIDATION_ERROR }}');
        
    - name: Determine target for override/update
      run: |
        echo "Searching for LINK_ID: $LINK_ID"

        # Look in links_other.json for a contentId match
        if jq -e --arg id "$LINK_ID" '.OTHER[]? | select(.contentId == $id)' src/links/links_other.json >/dev/null 2>&1; then
          echo "FOUND_IN=links_other.json" >> $GITHUB_ENV
          echo "ACTION=edit_direct" >> $GITHUB_ENV
          echo "TARGET_FILE=src/links/links_other.json" >> $GITHUB_ENV
          echo "TARGET_KEY=OTHER" >> $GITHUB_ENV
          echo "Found in src/links/links_other.json; will update that file."

        # Else look in links_af.json for a contentId match
        elif jq -e --arg id "$LINK_ID" '.afpCategorizedLinksDto.links[]? | .[]? | select(.contentId == $id)' src/links/links_af.json >/dev/null 2>&1; then
          echo "FOUND_IN=links_af.json" >> $GITHUB_ENV
          echo "ACTION=override" >> $GITHUB_ENV
          echo "Found in src/links/links_af.json; will add override entry."

        # Else look in links_unofficial.json for a contentId match
        elif jq -e --arg id "$LINK_ID" '.UNOFFICIAL[]? | select(.contentId == $id)' src/links/links_unofficial.json >/dev/null 2>&1; then
          echo "FOUND_IN=links_unofficial.json" >> $GITHUB_ENV
          echo "ACTION=edit_direct" >> $GITHUB_ENV
          echo "TARGET_FILE=src/links/links_unofficial.json" >> $GITHUB_ENV
          echo "TARGET_KEY=UNOFFICIAL" >> $GITHUB_ENV
          echo "Found in src/links/links_unofficial.json; will update that file."

        else
          echo "FOUND_IN=none" >> $GITHUB_ENV
          echo "ACTION=none" >> $GITHUB_ENV
          echo "Error: LINK_ID '$LINK_ID' not found in links_other.json, links_af.json, or links_unofficial.json"
          exit 1
        fi

    - name: "Case: Editing a link in place (OTHER or UNOFFICIAL)"
      if: env.ACTION == 'edit_direct'
      run: |
        set -o pipefail
        jq -r --arg id "${{ env.LINK_ID }}" \
           --arg NEW_TITLE "${{ env.NEW_TITLE }}" \
           --arg NEW_LINK "${{ env.NEW_LINK }}" \
           --arg KEY "${{ env.TARGET_KEY }}" \
           '.[$KEY] |= map( if .contentId == $id then
               (if $NEW_TITLE != "" and $NEW_TITLE != "null" and $NEW_TITLE != "_No response_" then .title = $NEW_TITLE else . end) |
               (if $NEW_LINK != "" and $NEW_LINK != "null" and $NEW_LINK != "_No response_" then .link = $NEW_LINK else . end)
             else . end )' "${{ env.TARGET_FILE }}" | sponge "${{ env.TARGET_FILE }}"

        echo "✅ Updated ${{ env.TARGET_FILE }} for LINK_ID '${{ env.LINK_ID }}'"

    - name: "Case: Overriding an AF Portal link in links_af.json via links_override.json"
      if: env.ACTION == 'override'
      run: |
        set -o pipefail
        OVERRIDE_ID=$(uuidgen)

        # Build override entry, only including title/link if provided
        OVERRIDE_ENTRY=$(jq -n \
          --arg OVERRIDE_ID "$OVERRIDE_ID" \
          --arg LINK_ID "${{ env.LINK_ID }}" \
          --arg NEW_TITLE "${{ env.NEW_TITLE }}" \
          --arg NEW_LINK "${{ env.NEW_LINK }}" \
          '{
            "id": $OVERRIDE_ID,
            "match_method": "Content ID",
            "match": $LINK_ID,
            "timestamp": now
          } | 
          if ($NEW_TITLE != "" and $NEW_TITLE != "null" and $NEW_TITLE != "_No response_") then .title = $NEW_TITLE else . end |
          if ($NEW_LINK != "" and $NEW_LINK != "null" and $NEW_LINK != "_No response_") then .link = $NEW_LINK else . end'
        )

        # Append to links_override.json
        jq --argjson ENTRY "$OVERRIDE_ENTRY" '. += [$ENTRY]' src/links/links_override.json | sponge src/links/links_override.json

        echo "✅ Override added to links_override.json"
        
    - name: Commit New Changes
      uses: EndBug/add-and-commit@v9
      with:
        message: "Override or update link from issue: ${{ env.LINK_ID }}"

    - name: Trigger next workflow
      uses: actions/github-script@v6
      with:
        script: |
          await github.rest.repos.createDispatchEvent({
            owner: context.repo.owner,
            repo: context.repo.repo,
            event_type: 'build',
            client_payload: {
              commit_sha: context.sha,
              message: 'Updated links from Issue'
            }
          });
        
    - name: Add success comment
      uses: actions/github-script@v6
      with:
        script: |
          await github.rest.issues.createComment({
            issue_number: context.payload.client_payload.original_issue,
            owner: context.payload.client_payload.triggered_by,
            repo: context.repo.repo,
            body: `✅ **Link updated successfully!** 
            
            The link "${{ env.LINK_ID }}" has been updated and will be active on the next site deployment.
            
            **Update Details:**
            - Link ID: ${{ env.LINK_ID }}
            - New Title: ${{ env.NEW_TITLE }}
            - New URL: ${{ env.NEW_LINK }}`
          })
